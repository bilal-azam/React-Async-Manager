// Add new event types and corresponding handlers
export const newEventTypes = {
    TYPE_A: 'Type A',
    TYPE_B: 'Type B'
};
export function handleNewEventType(event) {
    console.log('Handling new event type:', event);
}