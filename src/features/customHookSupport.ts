// Enable users to define their own custom hooks
export function allowCustomHooks(definition) {
    // Implement functionality to register user-defined hooks
    console.log('Custom Hook support added');
}